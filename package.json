{
  "name": "jsdoccomments",
  "displayName": "JSDocComments",
  "description": "Add JSDocs and inline comments to the selected code",
  "version": "0.0.1",
  "publisher": "Gabriel Estrada",
  "main": "./dist/extension.js",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "engines": {
    "vscode": "^1.108.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:jsdoccomments.runJsdocOnSelection",
    "onCommand:jsdoccomments.generateJsdocForSelection"
  ],
  "contributes": {
    "commands": [
      {
        "command": "jsdoccomments.runJsdocOnSelection",
        "title": "Copilot: Apply JSDoc (/jsdoc) for Selection/Enclosing Symbol"
      },
      {
        "command": "jsdoccomments.generateJsdocForSelection",
        "title": "Copilot: Generate JSDoc Text Only (/jsdoc_text)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "jsdoccomments.runJsdocOnSelection",
          "when": "editorTextFocus && (resourceLangId == typescript || resourceLangId == javascript || resourceLangId == typescriptreact || resourceLangId == javascriptreact)",
          "group": "navigation@1000"
        },
        {
          "command": "jsdoccomments.generateJsdocForSelection",
          "when": "editorTextFocus && (resourceLangId == typescript || resourceLangId == javascript || resourceLangId == typescriptreact || resourceLangId == javascriptreact)",
          "group": "navigation@1001"
        }
      ]
    },
    "keybindings": [
      {
        "command": "jsdoccomments.runJsdocOnSelection",
        "key": "ctrl+alt+j",
        "when": "editorTextFocus && (resourceLangId == typescript || resourceLangId == javascript || resourceLangId == typescriptreact || resourceLangId == javascriptreact)"
      },
      {
        "command": "jsdoccomments.generateJsdocForSelection",
        "key": "ctrl+alt+shift+j",
        "when": "editorTextFocus && (resourceLangId == typescript || resourceLangId == javascript || resourceLangId == typescriptreact || resourceLangId == javascriptreact)"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.108.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.52.0",
    "eslint": "^9.39.2",
    "esbuild": "^0.27.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
